⚡ The 5-Step Query Flow (GraphRAG Only)
STEP 1 ──► USER QUERY ENTERS
           "What treatment protocols reduce mortality
            in elderly patients with comorbidities?"

STEP 2 ──► GRAPH TRAVERSAL ACTIVATES
           [MORTALITY] → [ELDERLY] → [COMORBIDITIES]
           → [TREATMENT PROTOCOLS] → [RISK FACTORS]
           Query travels the relationship web simultaneously

STEP 3 ──► COMMUNITY DETECTION
           System identifies the most relevant
           connected node cluster as a glowing sub-graph

STEP 4 ──► GLOBAL REASONING
           ALL activated nodes synthesized
           simultaneously — not sequentially

STEP 5 ──► SINGLE SYNTHESIZED ANSWER
           "Protocol X reduces mortality 34%
            EVIDENCE: 847 connected data points
            REASONING CHAIN: [visible path]
            CONFIDENCE: 94.7%"


🛡️ Why The Algorithm Cannot Consume GraphRAG
Attack Type	Traditional DB	Vector DB	GraphRAG
File extraction	✅ Succeeds instantly	⚠️ Partially succeeds	❌ FAILS — node held by edges
Fragmentation	✅ Trivial — already fragmented	⚠️ Cluster isolation possible	❌ FAILS — web is indivisible
Pattern harvesting	✅ Complete exposure	⚠️ Embedding patterns exposed	❌ FAILS — on-premise, invisible
Cloud interception	✅ Direct access	✅ API query harvesting	❌ FAILS — zero cloud contact
Consumption	✅ Complete	⚠️ Partial	❌ ERROR: PROTOCOL FAILURE
"To destroy one node, it would have to destroy ALL. And destroying all destroys its own learning foundation." — The Algorithm, EP.5

📋 Vector Protocol — Full Specification
╔══════════════════════════════════════════════════════════╗
║         VECTOR PROTOCOL — GRAPHRAG SPECIFICATIONS        ║
╠══════════════════════════════════════════════════════════╣
║  Architecture:    Knowledge Graph — Nodes + Edges        ║
║  Deployment:      On-Premise ONLY                        ║
║  Cloud Exposure:  ZERO                                   ║
║  Query Model:     Global relationship reasoning          ║
║  Answer Model:    Single synthesized response            ║
║  Context Window:  Entire knowledge graph simultaneously  ║
║  Growth Model:    Exponential — stronger every query     ║
║  Ownership:       100% yours. Always.                    ║
║  Threat Vector:   NONE — immune to fragmentation         ║
║  MCP Bridges:     All tools connected locally            ║
╠══════════════════════════════════════════════════════════╣
║  "Not a database. Not a search engine. A MEMORY."        ║
║  "YOUR memory. On YOUR machine. Under YOUR control."     ║
╠══════════════════════════════════════════════════════════╣
║  CORTEX GRAPHRAG PROTOCOL × HANGARX PRODUCTIONS         ║
║              YOUR DATA. YOUR RULES.                      ║
╚══════════════════════════════════════════════════════════╝